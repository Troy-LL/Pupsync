# Agent Context

Shared context for humans and agents working in this repo.
All product truth lives in `docs/`. Reference those files when prompting agents.
Keep this folder lean — add files only when they provide durable cross-session context.
