# .cursor/

Tooling slots for Cursor IDE. Product truth lives in `docs/` — reference those files, not this folder.

| Path | Purpose |
|------|---------|
| `commands/` | Custom slash commands — manage via Cursor `/commands` |
| `rules/` | AI coding rules (`.mdc`) — manage via Cursor `/rules` |
| `skills/` | Agent skill definitions (`SKILL.md`) |
| `docs/` | This file — Cursor tooling orientation |
